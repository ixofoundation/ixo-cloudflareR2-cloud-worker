{
  "name": "ixo-r2-cloud-worker",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev ./src/index.ts",
    "deploy": "wrangler publish ./src/index.ts",
    "build": "wrangler publish --dry-run --outdir=dist"
  },
  "license": "MIT",
  "dependencies": {
    "hono": "^2.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.10.0",
    "miniflare": "^2.5.1",
    "wrangler": "^2.0.24"
  }
}
